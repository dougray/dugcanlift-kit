// Placeholder so SwiftPM does not reject this target as "empty" while it
// backs a library product before a later task lands real sources here.
// Not called for by task-1-brief.md, which specifies only `.gitkeep` — see
// task-1-2-report.md for why it was added.
